<?php
	
	/*$pdo = new PDO("mysql:host=localhost;dbname=login", "root", "");
	$inserir = $pdo->prepare("SELECT COUNT(nome) AS total FROM usuario WHERE nome = 'Ricardo Oliveira Sousa Filho';");
	$inserir->execute();
	foreach ($inserir as $key) {
		echo $key['total'];
		if ($key['total'] == 0){
			echo "inserido";
		}else{
			echo "já existe";
		}
	}*/
	
?>